@import "reset-context";
@import "style-portal";

.tooltip-container {
    reset-context();

    display: inline-block;

    .tooltip {
        display: none;

        &.show {
            display: inline-block;
        }
    }

    .tooltip-target-container {
        display: inline-block;
    }
}
